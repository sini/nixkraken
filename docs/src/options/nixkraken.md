# Root options

Nixkraken exposes its options under `programs.nixkraken`.

Some options, listed below, are available at the root level, while others are available as scoped options under the root level.

For example, the [`commitGraph`](./commitGraph.md) options are available at `programs.nixkraken.commitGraph`.
